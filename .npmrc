# Some ecosystem packages (e.g. react-i18next) still declare an optional
# TypeScript ^5 peer, while Expo SDK 57 ships TypeScript 6. These are optional
# peer ranges lagging behind the toolchain, not real incompatibilities, so we
# relax npm's strict peer resolution for reproducible installs.
legacy-peer-deps=true
